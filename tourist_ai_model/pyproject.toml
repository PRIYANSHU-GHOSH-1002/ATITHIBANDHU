[project]
name = "atithibandhu-ai"
version = "1.0.0"
requires-python = ">=3.12,<3.15"
dependencies = [
    "fastapi==0.141.1",
    "numpy",
    "pandas",
    "pydantic"
]

[tool.vercel]
entrypoint = "api:app"
